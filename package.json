{
  "name": "ecosphere-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\" \"cd ml-service && uvicorn app.main:app --reload --port 8000\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
